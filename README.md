# PUT bot v1

1. Copy `.env.example` to `.env`
2. Fill `BOT_TOKEN`
3. `npm install`
4. `npm start`

Контент бот читает из Google Sheets. Если таблица временно недоступна, бот использует встроенный fallback-снимок.
